//generelle Argumente um das Navigieren zu erleichtern 
class GeneralArguments {
  static String cameraName = "";
  static String imagePath = "";

  // get getCamera => camera;
  // set setCamera(camera) => this.camera = camera;

  // get getCameraName => cameraName;
  // set setCameraName(cameraName) => this.cameraName = cameraName;

  // get getImagePath => imagePath;
  // set setImagePath(imagePath) => this.imagePath = imagePath;

  // GeneralArguments(this.camera, this.cameraName, this.imagePath);
}
